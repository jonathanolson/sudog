#ifndef BACKTRACK_TO_BITS
#define BACKTRACK_TO_BITS

#include "backtrack_common.h"
#include "bits.h"

void backtrack_to_bits(Grid& grid, Bits* bits);
void backtrack_of_bits(Grid& grid, Bits* bits);

#endif
